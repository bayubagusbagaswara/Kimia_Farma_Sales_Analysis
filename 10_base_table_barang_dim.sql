create table barang_dim (
    id_barang varchar(9),
    nama_barang varchar(50),
    kemasan varchar(6),
    harga int,
    nama_tipe varchar(5),
    kode_brand int,
    brand varchar(8),
    primary key (id_barang)
);