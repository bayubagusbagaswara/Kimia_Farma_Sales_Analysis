create table barang_dim (
    id_barang varchar(9),
    nama_barang varchar(50),
    sektor varchar(1),
    tipe varchar(11),
    brand varchar(10),
    kemasan varchar(6),
    harga int,
    primary key (id_barang)
);