create table `barang_pre` (
    `id_barang` varchar(7),
    `nama` varchar(41),
    `nama_tipe` varchar(11),
    `kemasan` varchar(6),
    `harga` int,
    `kode_brand` int,
    `nama_brand` varchar(8),
    primary key (id_barang)
);